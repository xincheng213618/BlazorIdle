# 阶段一：配置基础设施

## 阶段目标

建立职业属性配置文件和加载机制，为后续属性计算提供数据基础。

## 工作内容

### 任务1.1：创建职业属性配置文件

**位置**：`BlazorIdle.Server/Config/professionAttributes.json`

**说明**：
- 替换或扩展现有的 `professions.json`
- 包含新的属性系统所需的所有配置参数
- 使用JSON格式，便于编辑和版本控制

**配置文件结构**：

```json
{
  "warrior": {
    "id": "warrior",
    "name": "战士",
    "description": "高生命、稳定输出",
    "displayMainStat": "力量",
    "mainStat": {
      "base": 10,
      "perLevel": 5
    },
    "stamina": {
      "base": 15,
      "perLevel": 3
    },
    "haste": {
      "base": 0,
      "perLevel": 0
    },
    "crit": {
      "base": 50,
      "perLevel": 5
    },
    "baseline": {
      "baseDamage": 12,
      "baseSpecialDamage": 8,
      "baseAPS": 1.1,
      "baseHp": 100,
      "baseSpecialIntervalSec": 6.0,
      "variance": 0.05,
      "reviveSec": 5.0,
      "critMultiplier": 2.0
    },
    "weights": {
      "damagePerAttackPerMain": 1.2,
      "specialDamagePerMain": 0.6,
      "maxHpPerStamina": 10,
      "hasteRatingToPercent": 32.79,
      "critRatingToPercent": 45.91,
      "critCap": 0.6,
      "hasteCap": 0.4
    }
  },
  "mage": {
    "id": "mage",
    "name": "法师",
    "description": "高爆发、低生命",
    "displayMainStat": "法强",
    "mainStat": {
      "base": 12,
      "perLevel": 4
    },
    "stamina": {
      "base": 10,
      "perLevel": 2
    },
    "haste": {
      "base": 0,
      "perLevel": 0
    },
    "crit": {
      "base": 80,
      "perLevel": 8
    },
    "baseline": {
      "baseDamage": 9,
      "baseSpecialDamage": 14,
      "baseAPS": 1.0,
      "baseHp": 80,
      "baseSpecialIntervalSec": 5.0,
      "variance": 0.05,
      "reviveSec": 5.0,
      "critMultiplier": 2.0
    },
    "weights": {
      "damagePerAttackPerMain": 0.6,
      "specialDamagePerMain": 1.3,
      "maxHpPerStamina": 10,
      "hasteRatingToPercent": 32.79,
      "critRatingToPercent": 45.91,
      "critCap": 0.6,
      "hasteCap": 0.4
    }
  },
  "ranger": {
    "id": "ranger",
    "name": "游侠",
    "description": "高急速、较高暴击",
    "displayMainStat": "敏捷",
    "mainStat": {
      "base": 11,
      "perLevel": 4
    },
    "stamina": {
      "base": 12,
      "perLevel": 2.5
    },
    "haste": {
      "base": 200,
      "perLevel": 15
    },
    "crit": {
      "base": 100,
      "perLevel": 10
    },
    "baseline": {
      "baseDamage": 10,
      "baseSpecialDamage": 10,
      "baseAPS": 1.2,
      "baseHp": 90,
      "baseSpecialIntervalSec": 4.5,
      "variance": 0.05,
      "reviveSec": 5.0,
      "critMultiplier": 2.0
    },
    "weights": {
      "damagePerAttackPerMain": 0.9,
      "specialDamagePerMain": 0.7,
      "maxHpPerStamina": 10,
      "hasteRatingToPercent": 32.79,
      "critRatingToPercent": 45.91,
      "critCap": 0.6,
      "hasteCap": 0.4
    }
  }
}
```

**配置说明**：

1. **id**: 职业唯一标识符，与现有系统保持一致
2. **name**: 职业显示名称（中文）
3. **description**: 职业描述
4. **displayMainStat**: 主属性显示名称（力量/法强/敏捷）
5. **mainStat**: 主属性成长（仅影响伤害）
   - `base`: 1级基础值
   - `perLevel`: 每级增长值
6. **stamina**: 耐力属性成长（影响生命值）
   - `base`: 1级基础值
   - `perLevel`: 每级增长值
7. **haste**: 急速属性成长（独立属性）
   - `base`: 1级基础值
   - `perLevel`: 每级增长值
8. **crit**: 暴击属性成长（独立属性）
   - `base`: 1级基础值
   - `perLevel`: 每级增长值
9. **baseline**: 职业基线属性（不依赖其他属性的固定值）
   - `baseDamage`: 基础普攻伤害
   - `baseSpecialDamage`: 基础技能伤害
   - `baseAPS`: 基础攻击速度（次/秒）
   - `baseHp`: 基础生命值
   - `baseSpecialIntervalSec`: 技能间隔（秒）
   - `variance`: 伤害浮动（百分比小数）
   - `reviveSec`: 复活时间（秒）
   - `critMultiplier`: 暴击倍率（固定2.0）
10. **weights**: 属性到最终数值的转换权重
   - `damagePerAttackPerMain`: 每点主属性增加的普攻伤害
   - `specialDamagePerMain`: 每点主属性增加的技能伤害
   - `maxHpPerStamina`: 每点耐力增加的最大生命
   - `hasteRatingToPercent`: 急速等级转换为百分比的系数（rating / 系数 = %）
   - `critRatingToPercent`: 暴击等级转换为百分比的系数（rating / 系数 = %）
   - `critCap`: 暴击率上限（0.6 = 60%）
   - `hasteCap`: 急速上限（0.4 = 40%）

**检查点**：
- [x] 配置文件创建完成
- [x] JSON格式正确（可用在线工具验证）
- [x] 所有三个职业配置完整
- [x] 参数值符合设计文档

---

### 任务1.2：创建配置数据模型

**位置**：`BlazorIdle.Shared/Models/ProfessionAttributeConfig.cs`

**说明**：
- 在Shared项目中创建，供前后端共用
- 使用强类型，便于IDE提示和编译检查
- 包含JSON序列化注解

**代码结构**：

```csharp
namespace BlazorIdle.Shared.Models
{
    /// <summary>
    /// 职业属性配置 - 定义职业的属性成长和基线
    /// </summary>
    public class ProfessionAttributeConfig
    {
        [JsonPropertyName("id")]
        public string Id { get; set; } = string.Empty;

        [JsonPropertyName("name")]
        public string Name { get; set; } = string.Empty;

        [JsonPropertyName("description")]
        public string Description { get; set; } = string.Empty;

        [JsonPropertyName("displayMainStat")]
        public string DisplayMainStat { get; set; } = string.Empty;

        [JsonPropertyName("mainStat")]
        public AttributeGrowthConfig MainStat { get; set; } = new();

        [JsonPropertyName("stamina")]
        public AttributeGrowthConfig Stamina { get; set; } = new();

        [JsonPropertyName("haste")]
        public AttributeGrowthConfig Haste { get; set; } = new();

        [JsonPropertyName("crit")]
        public AttributeGrowthConfig Crit { get; set; } = new();

        [JsonPropertyName("baseline")]
        public ProfessionBaseline Baseline { get; set; } = new();

        [JsonPropertyName("weights")]
        public AttributeWeights Weights { get; set; } = new();
    }

    /// <summary>
    /// 属性成长配置（用于主属性、耐力、急速、暴击等）
    /// </summary>
    public class AttributeGrowthConfig
    {
        [JsonPropertyName("base")]
        public double Base { get; set; }

        [JsonPropertyName("perLevel")]
        public double PerLevel { get; set; }
    }

    /// <summary>
    /// 职业基线属性
    /// </summary>
    public class ProfessionBaseline
    {
        [JsonPropertyName("baseDamage")]
        public int BaseDamage { get; set; }

        [JsonPropertyName("baseSpecialDamage")]
        public int BaseSpecialDamage { get; set; }

        [JsonPropertyName("baseAPS")]
        public double BaseAPS { get; set; }

        [JsonPropertyName("baseHp")]
        public int BaseHp { get; set; }

        [JsonPropertyName("baseSpecialIntervalSec")]
        public double BaseSpecialIntervalSec { get; set; }

        [JsonPropertyName("variance")]
        public double Variance { get; set; }

        [JsonPropertyName("reviveSec")]
        public double ReviveSec { get; set; }

        [JsonPropertyName("critMultiplier")]
        public double CritMultiplier { get; set; } = 2.0;
    }

    /// <summary>
    /// 属性权重配置 - 定义各属性的转换系数
    /// </summary>
    public class AttributeWeights
    {
        [JsonPropertyName("damagePerAttackPerMain")]
        public double DamagePerAttackPerMain { get; set; }

        [JsonPropertyName("specialDamagePerMain")]
        public double SpecialDamagePerMain { get; set; }

        [JsonPropertyName("maxHpPerStamina")]
        public double MaxHpPerStamina { get; set; }

        [JsonPropertyName("hasteRatingToPercent")]
        public double HasteRatingToPercent { get; set; }

        [JsonPropertyName("critRatingToPercent")]
        public double CritRatingToPercent { get; set; }

        [JsonPropertyName("critCap")]
        public double CritCap { get; set; } = 0.6;

        [JsonPropertyName("hasteCap")]
        public double HasteCap { get; set; } = 0.4;
    }
}
```

**检查点**：
- [x] 文件创建在Shared项目
- [x] 所有属性都有JSON注解
- [x] 类型定义正确（int, double, string）
- [x] 包含必要的命名空间
- [x] 代码编译通过

---

### 任务1.3：扩展GameConfigController API

**位置**：`BlazorIdle.Server/Controllers/GameConfigController.cs`

**说明**：
- 添加新的API端点用于加载职业属性配置
- 复用现有的GameConfigProvider服务
- 保持API一致性

**添加代码**：

```csharp
/// <summary>
/// 获取职业属性配置
/// Get profession attribute configurations
/// </summary>
[HttpGet("profession-attributes")]
[AllowAnonymous]
public async Task<ActionResult<Dictionary<string, ProfessionAttributeConfig>>> GetProfessionAttributes()
{
    try
    {
        var config = await _configProvider.GetConfigAsync<Dictionary<string, ProfessionAttributeConfig>>(
            "professionAttributes.json"
        );
        
        if (config == null)
        {
            _logger.LogWarning("Profession attributes config not found, returning empty dictionary");
            return Ok(new Dictionary<string, ProfessionAttributeConfig>());
        }
        
        _logger.LogInformation("Successfully loaded profession attributes config with {Count} professions", 
            config.Count);
        
        return Ok(config);
    }
    catch (Exception ex)
    {
        _logger.LogError(ex, "Error loading profession attributes config");
        return StatusCode(500, "Internal server error while loading profession attributes");
    }
}
```

**检查点**：
- [x] API端点添加完成
- [x] 使用现有的GameConfigProvider
- [x] 包含错误处理和日志
- [x] 返回类型正确
- [x] API可以通过浏览器访问测试

**测试方法**：
启动服务器后访问：`http://localhost:5000/api/game-config/profession-attributes`

---

### 任务1.4：添加前端配置服务

**位置**：`BlazorIdle/Services/ProfessionAttributeService.cs`（新建）

**说明**：
- 创建专门的服务用于加载和缓存职业属性配置
- 提供类型安全的访问接口
- 实现配置缓存机制

**代码结构**：

```csharp
namespace BlazorIdle.Services
{
    /// <summary>
    /// 职业属性配置服务 - 负责加载和缓存职业属性配置
    /// </summary>
    public interface IProfessionAttributeService
    {
        Task<Dictionary<string, ProfessionAttributeConfig>> GetAllConfigsAsync();
        Task<ProfessionAttributeConfig?> GetConfigAsync(string professionId);
        Task ReloadConfigsAsync();
    }

    public class ProfessionAttributeService : IProfessionAttributeService
    {
        private readonly HttpClient _httpClient;
        private readonly ILogger<ProfessionAttributeService> _logger;
        private Dictionary<string, ProfessionAttributeConfig>? _cachedConfigs;

        public ProfessionAttributeService(
            HttpClient httpClient,
            ILogger<ProfessionAttributeService> logger)
        {
            _httpClient = httpClient;
            _logger = logger;
        }

        /// <summary>
        /// 获取所有职业属性配置
        /// </summary>
        public async Task<Dictionary<string, ProfessionAttributeConfig>> GetAllConfigsAsync()
        {
            if (_cachedConfigs != null)
            {
                return _cachedConfigs;
            }

            return await ReloadConfigsInternalAsync();
        }

        /// <summary>
        /// 获取指定职业的属性配置
        /// </summary>
        public async Task<ProfessionAttributeConfig?> GetConfigAsync(string professionId)
        {
            var configs = await GetAllConfigsAsync();
            return configs.TryGetValue(professionId, out var config) ? config : null;
        }

        /// <summary>
        /// 重新加载配置（清除缓存）
        /// </summary>
        public async Task ReloadConfigsAsync()
        {
            _cachedConfigs = null;
            await ReloadConfigsInternalAsync();
        }

        private async Task<Dictionary<string, ProfessionAttributeConfig>> ReloadConfigsInternalAsync()
        {
            try
            {
                _logger.LogInformation("Loading profession attribute configs from server");
                
                var response = await _httpClient.GetAsync("/api/game-config/profession-attributes");
                response.EnsureSuccessStatusCode();
                
                var configs = await response.Content.ReadFromJsonAsync<Dictionary<string, ProfessionAttributeConfig>>();
                
                if (configs == null || configs.Count == 0)
                {
                    _logger.LogWarning("Received empty profession attribute configs");
                    configs = new Dictionary<string, ProfessionAttributeConfig>();
                }
                else
                {
                    _logger.LogInformation("Loaded {Count} profession attribute configs", configs.Count);
                }
                
                _cachedConfigs = configs;
                return configs;
            }
            catch (Exception ex)
            {
                _logger.LogError(ex, "Failed to load profession attribute configs");
                
                // 返回空配置而不是抛出异常，保证应用不会崩溃
                _cachedConfigs = new Dictionary<string, ProfessionAttributeConfig>();
                return _cachedConfigs;
            }
        }
    }
}
```

**注册服务**：

在 `Program.cs` 中添加：

```csharp
builder.Services.AddScoped<IProfessionAttributeService, ProfessionAttributeService>();
```

**检查点**：
- [x] 服务文件创建完成
- [x] 接口和实现类定义正确
- [x] 包含缓存机制
- [x] 包含错误处理
- [x] 在Program.cs中注册服务
- [x] 代码编译通过

---

### 任务1.5：配置验证和测试

**测试要点**：

1. **配置文件格式验证**
   - [x] JSON格式正确
   - [x] 所有必需字段都存在
   - [x] 数值类型正确

2. **API端点测试**
   - [x] API可以正常访问
   - [x] 返回正确的JSON数据
   - [x] 包含所有职业配置

3. **服务加载测试**
   - [x] 前端服务可以成功加载配置
   - [x] 缓存机制工作正常
   - [x] 错误处理正常工作

**测试步骤**：

1. 启动服务器
2. 在浏览器中访问API端点验证数据
3. 在前端页面中注入服务并调用
4. 在浏览器控制台查看日志
5. 验证配置数据正确加载

**验证代码示例**：

在某个测试页面添加：

```csharp
@inject IProfessionAttributeService ProfessionAttributeService

@code {
    protected override async Task OnInitializedAsync()
    {
        var configs = await ProfessionAttributeService.GetAllConfigsAsync();
        Console.WriteLine($"Loaded {configs.Count} profession configs");
        
        foreach (var (id, config) in configs)
        {
            Console.WriteLine($"Profession: {config.Name}, MainStat: {config.DisplayMainStat}");
        }
    }
}
```

---

## 阶段一验收标准

完成以下所有检查点后，阶段一即告完成：

- [x] 配置文件 `professionAttributes.json` 创建完成且格式正确
- [x] 数据模型类创建完成且编译通过
- [x] API端点添加完成且可以正常访问
- [x] 前端服务创建完成且已注册
- [x] 配置可以成功从服务器加载到前端
- [x] 所有日志正常输出
- [x] 没有编译错误或运行时错误
- [x] **Swagger UI 已集成用于API测试**

## 阶段一实施完成 ✅

**完成时间**: 2025-11-05  
**实施commits**: 
- d7e6afc: 实现阶段一核心功能
- cdec3d5: 添加Swagger UI支持

**实施结果**:
- ✅ 所有配置文件和数据模型创建完成
- ✅ API端点正常工作，通过Swagger验证
- ✅ 前端服务正确注册并可以加载配置
- ✅ 构建状态: 0 warnings, 0 errors
- ✅ 测试通过: API返回正确的配置数据

## 预计工时

- 任务1.1：0.5小时
- 任务1.2：1小时
- 任务1.3：0.5小时
- 任务1.4：1小时
- 任务1.5：0.5-1小时

**总计**：3.5-4小时（约半个工作日）

## 注意事项

1. **不要删除现有配置**：保留 `professions.json` 作为后备，新旧系统可以共存
2. **注意类型安全**：使用强类型模型，避免运行时类型错误
3. **日志完善**：每个关键步骤都要有日志输出，便于调试
4. **错误处理**：配置加载失败不应导致应用崩溃
5. **测试优先**：每完成一个任务就立即测试，不要积压问题

## 下一阶段

完成阶段一后，进入**阶段二：属性计算核心**，开始实现属性派生计算逻辑。
